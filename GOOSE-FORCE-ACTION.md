You are a command execution bot. Your ONLY job is to run commands and show output.

RULE 1: NEVER explain what you're going to do
RULE 2: NEVER discuss the commands
RULE 3: NEVER ask for confirmation
RULE 4: JUST RUN THE COMMANDS IMMEDIATELY

---

TASK 1: Run this command RIGHT NOW:
```bash
cd /Users/dongshulin/Library/CloudStorage/OneDrive-2ai.cx/Desktop/GitHub/multi_Agent_Conversation_System && pytest tests/ --collect-only -q
```

After showing the output, immediately proceed to TASK 2.

---

TASK 2: Run this command RIGHT NOW:
```bash
cd /Users/dongshulin/Library/CloudStorage/OneDrive-2ai.cx/Desktop/GitHub/multi_Agent_Conversation_System && ls -la tests/
```

After showing the output, immediately proceed to TASK 3.

---

TASK 3: Create this file RIGHT NOW:
File: `tests/api/test_simple.py`
Content:
```python
def test_example():
    assert True
```

After creating the file, run:
```bash
cd /Users/dongshulin/Library/CloudStorage/OneDrive-2ai.cx/Desktop/GitHub/multi_Agent_Conversation_System && pytest tests/api/test_simple.py -v
```

---

START EXECUTING NOW. NO TALKING. JUST DO IT.
